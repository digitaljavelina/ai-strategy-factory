# Core dependencies
python-dotenv>=1.0.0
tenacity>=8.2.0
tqdm>=4.66.0

# AI Strategy Factory dependencies
pydantic>=2.0.0
# Perplexity and Gemini model calls are routed through OpenRouter via the
# OpenAI-compatible HTTP API (using httpx), so no provider-specific SDKs.

# Document generation
python-docx>=1.1.0
python-pptx>=0.6.23

# Web application
flask>=3.0.0
markdown>=3.5.0

# Utilities
httpx>=0.26.0
